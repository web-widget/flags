import { expect, it, describe } from 'vitest';
import { FlagDefinitions } from '.';

describe('FlagDefinitions', () => {
  it('is a function', () => {
    expect(typeof FlagDefinitions).toBe('function');
  });
});
